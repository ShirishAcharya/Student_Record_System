# Student_Record_System

This is a simple student record system allowing the user to add, sort, delete and display the records. The records are written to a text file so that it can be used
later as well.
